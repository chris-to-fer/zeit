// import React from "react";

// export default function dateDisplayFormat({ mongo }) {
//   let day = mongo?.slice(8, 10);
//   let month = mongo?.slice(5, 7);
//   let year = mongo?.slice(2, 4);
//   return day + "." + month + "." + year;
// }
